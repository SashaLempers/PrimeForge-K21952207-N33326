# Minimal raw campaign material

This directory contains only the raw records needed to connect the selected
range, sieve output, completed result prefix, checkpoint, proof log, and
integrity checks:

- `campaign.json`
- `checkpoint.json`
- `survivors.txt`
- `results.tsv`
- `campaign-integrity.json`
- `short-validation-summary.json`
- `short-validation-oracle-comparison.tsv`
- `telemetry-summary.json`
- `preserved-tool-hashes.txt`

The full private dossier contains 256 hashed artifacts. This public export does
not duplicate 22 unit telemetry streams, watchdog logs, all pending batch files,
third-party binaries, private repository history, or third-party web-response
bodies. Those omissions do not remove the candidate, witness, exact modular
verification, result prefix, or public-audit manifests needed to inspect the
scientific claim.

No binary executable or DLL is distributed here.
